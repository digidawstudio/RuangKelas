<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.digidaw.ruangkelas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Chat.ChatActivity" />
        <activity android:name=".Absensi.AbsensiActivity" />
        <activity android:name=".Profile.ProfileActivity" />
        <activity android:name=".Student.StudentActivity" />
        <activity android:name=".Profile.AccountSettingActivity" />
        <activity android:name=".Utilitas.StartActivity"></activity>
    </application>

</manifest>